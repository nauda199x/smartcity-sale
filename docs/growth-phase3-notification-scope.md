# Notification scope

Phase 3 provides a private in-account new-match indicator. It does not send unsolicited or external email/push notifications. The saved-search schema and `last_seen_at` model are intentionally reusable by a future opt-in delivery worker, but that delivery channel is outside this release.
